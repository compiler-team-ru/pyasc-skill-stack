# `legacy-cann-mirror-wip/` — quarantined Stage 3.3 + 3.4 evidence

These 80 files were generated by `collect_generative_evidence.py` during
the original Stage 3.3 (48 cells) and Stage 3.4 (32 trials) runs while
the harness was importing `asc` / `asc2` from
`/home/aloschilov/workspace/pyasc` — a stale local clone of the
`gitcode.com/cann/pyasc` mirror on the `wip-matmul-sync-and-reduce-fuse`
branch (HEAD `345e13c`). That codebase is **not** the team baseline.

The canonical evaluation now imports from
`/home/aloschilov/workspace/pyasc-v2-eval`, a dedicated read-only clone
of `gitcode.com/compiler-team/pyasc` pinned to `v2 @ 7b85554a`. The
re-run of these 80 cells lives directly under `evidence/`.

These files are kept for forensic comparison (see the
"matmul-WIP-tree vs v2-eval drift" section of
`docs/skill-value-q1-findings.md`) but must not feed into the current
aggregator. `tools/compare_skills_value.py` skips any path containing
`legacy-cann-mirror-wip` for that reason; the `tests/unit/tools/test-skills-value-smoke.sh`
schema-v4 gate excludes the same prefix.

Subdirectories:

- `stage33/` — 48 base protocol files (`*-cloud-default-p[2-6].json`).
- `stage34/` — 32 stability-sweep trials (`*-cloud-default-p[2-6]-r[2-3].json`).
